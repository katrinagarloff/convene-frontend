const defaultState = {
  user: {
    lat: 40.700518,
    lon: -73.929678
    },
  meetUps: []
}

export const reducer = (state = defaultState, action) => {
  switch (action.type) {
    default:
    return state
  }
}
